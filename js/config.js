/**
 * config.js — Configuración global del frontend
 *
 * Único archivo que hay que tocar para apuntar a otra API.
 * En desarrollo: http://localhost:3000
 * En producción: URL de Railway
 */

const CONFIG = {
  API_URL: 'https://app-validaciones-production.up.railway.app',   // ← cambiar por tu URL de Railway
};
